# Local development
VITE_API_BASE_URL=/api
VITE_PROXY_TARGET=http://localhost:3000

# Production client requests should stay same-origin.
VITE_API_BASE_URL=/api

# Vercel runtime-only environment variable for the proxy function.
# Set this in the frontend Vercel project settings instead of exposing it to the browser.
# BACKEND_API_BASE_URL=https://your-backend-project.vercel.app
