# Local development
VITE_API_BASE_URL=/api
VITE_PROXY_TARGET=http://localhost:3000

# Production client requests should stay same-origin.
VITE_API_BASE_URL=/api

# Vercel runtime-only environment variables for frontend serverless routes.
# Set these in the frontend Vercel project settings instead of exposing them to the browser.
# BACKEND_API_BASE_URL=https://your-backend-project.vercel.app
# BLOB_READ_WRITE_TOKEN=vercel_blob_rw_xxx
